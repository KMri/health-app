<%= form_for(@article) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  
  <%= f.label :description %>
  <%= f.text_area :description %>
  
  <%= button_tag('投稿', {class: "btn waves-effect waves-light orange",
                                   type: "submit"}) %>

<% end %>